export type ClientRects = {
    bottom: number,
    height: number,
    left: number,
    right: number,
    top: number,
    width: number,
    x: number,
    y: number,
}
export type Offset = {
    x: number,
    y: number
}
